PORT=3000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/eof_talent"
JWT_ACCESS_SECRET="replace-me-access-secret"
JWT_REFRESH_SECRET="replace-me-refresh-secret"
JWT_ACCESS_EXPIRES="15m"
JWT_REFRESH_EXPIRES="30d"

# --- Media storage ---
# Default (recommended local): files saved under backend/uploads — no S3 needed
STORAGE_DRIVER=local
UPLOAD_DIR=./uploads

# AWS S3 (optional). Prefer S3_* names used by the app:
# STORAGE_DRIVER=s3
# S3_REGION=ap-south-1
# S3_BUCKET=eof-talent-images
# S3_ACCESS_KEY=AKIA...
# S3_SECRET_KEY=...
# S3_ENDPOINT=https://s3.ap-south-1.amazonaws.com
# S3_FORCE_PATH_STYLE=false
# AWS_* aliases are also accepted.

OTP_TEST_BYPASS=true
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
