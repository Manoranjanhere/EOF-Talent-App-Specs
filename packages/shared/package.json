{
  "name": "@eof/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No lints configured for shared\"",
    "test": "echo \"No tests configured for shared\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
